# Json to Tree Hierarchy Viewer
То, что написано на упаковке. Киселев Всеволод.

Файл должен лежать по адресу 'D:/Dataset' и называться tate.json

Установка и запуск через setuptools:
```bash
cd 'куда положили файлы'
pip install .
tate
```
